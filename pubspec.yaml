name: flozendmemory
description: 思い出掘り起こしアプリ - 永久凍土に封印された思い出を発掘せよ
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  firebase_auth: ^6.1.3
  firebase_storage: ^13.0.5

  uuid: ^4.5.2
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  intl: ^0.20.2
  http: ^1.2.0
  cached_network_image: ^3.4.1
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

